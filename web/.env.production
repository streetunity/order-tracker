# Client-side API URLs (accessible in browser)
# Using relative path since Nginx proxies /api/* to backend
NEXT_PUBLIC_API_BASE=
NEXT_PUBLIC_API_URL=

# Server-side API URL (for Next.js server-side rendering)
# Points to localhost since Nginx handles external routing
API_URL=http://localhost:4000

# App settings
NODE_ENV=production
